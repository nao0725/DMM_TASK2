 
<!-- 【HTML】ソースコード -->

<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <!-- Font Awesomeファイルの読み込み -->

    <!-- CSSファイルの読み込み -->
    <link rel="stylesheets" href="application.css" />
    <title>index</title>
  </head>

  <body>
  <p class="notice"></p>
  
  <h1>Books</h1>
  
  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Body</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    
    <tbody>
      <tr>
        <% @books.each do |book| %>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
        <td><span><%= link_to "Show", book_path(book.id) %></span></td>
        <td><span><%= link_to "Edit", edit_book_path(book.id) %></span></td>
        <td><span><%= link_to "Destroy", book_path(book.id),method: :delete %></span></td>
      <% end %>
      </tr>
    </tbody>
  </table>
  
   <h1>New book</h1>
   
    <% if @book.errors.any? %>
        <%= @book.errors.count %>件のエラーが発生しました
      <% @book.errors.full_messages.each do |message| %>
        <%= message %>
      <% end %>
    <% end %>
    
  <form>
  <%= form_with model: @book,url:'/books', local:true do |f| %>
  </from>
  
  <div class ="field" >
    <h4 class=book_title>Title</h4>
    <%=f.text_field :title %>
  </div>
  
  <div class= "field">
    <h4 class= "book_body">Body</h4>
    <%=f.text_area :body %>
  </div>
  
  <div class="actions">
  <%=f.submit 'Create Book' %>
  </div>
  
    <% end %>
  </form>
  </body>
</html>

