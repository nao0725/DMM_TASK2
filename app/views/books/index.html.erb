 
<!-- 【HTML】ソースコード -->

<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <!-- Font Awesomeファイルの読み込み -->

    <!-- CSSファイルの読み込み -->
    <link rel="stylesheets" href="index.css" />
    <title>index</title>
  </head>

  <body>
  <h1>Books</h1>
  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Body</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
      <p>タイトル</p>
      <span><%= book.title %></span>
      <span><%= book.body %></span>
      <span><%= link_to "Show", book_path(book.id) %></span>
      <span><%= link_to "Edit", edit_book_path(book.id) %></span>
      <span><%= link_to "Destroy", book_path(book.id),method: :delete %></span>
      <% end %>
    </tbody>
  </table>
  
   <h1>New book</h1>
  
  <%= form_with model: @book,url:'/books', local:true do |f| %>

  <h4>Title</h4>
  <%=f.text_field :title %>
  
  <h4>Body</h4>
  <%=f.text_area :body %>
  <%=f.submit 'Create Book' %>
  
    <% end %>
  </div>
  </form>
  </body>
</html>